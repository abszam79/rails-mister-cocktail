<div class="banner" style="background-image: linear-gradient(-225deg, rgba(0,101,168,0.6) 0%, rgba(0,36,61,0.6) 50%), url('<%= cl_image_path @cocktail.photo, height: 300, width: 400, crop: :fill %>');">
  <div class="banner-content">
    <h1><%= @cocktail.name %></h1>
    <p>Learn to drink, change cocktail</p>
  </div>
</div>


<div class="container">
  <div class="row">
    <h2><%= @cocktail.name %></h2>
    <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Iure molestiae sunt, quidem fugit laborum, sapiente quibusdam eaque consectetur vitae ratione magnam rem illum tenetur cupiditate in eos? Hic, laudantium, nobis.</p>
    <div class="box">

    <p><% @cocktail.doses.each do |dose| %></p>
    <%= link_to ' <i class="fa fa-trash" aria-hidden="true"></i>'.html_safe, dose_path(dose),
              method: :delete,
              data: { confirm: "Are you sure?" }, class: "btn btn-primary" %>
    <%= dose.description %>
    <%= dose.ingredient.name %>
    <% end %>
   </div>
 <br />
    <%= link_to "Back to list", cocktails_path %>
    <br /><br />
  </div>

  <div class="row">
    <div class="formulaire">
      <%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient, collection: Ingredient.all.order('name ASC') %>
  <%= f.button :submit, class: "btn btn-success" %>
<% end %>


<%= simple_form_for [@cocktail] do |f| %>
<h3>Pick a new Pic</h3>
  <%= f.input :photo %>
  <%= f.input :photo_cache, as: :hidden %>
  <%= f.button :submit, class: "btn btn-success" %>
<% end %>
    </div>

  </div>


</div>
    <br /><br />
