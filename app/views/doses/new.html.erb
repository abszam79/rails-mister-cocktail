<div class="container">
  <div class="row">

    <%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient, collection: Ingredient.all.order('name ASC') %>
  <%= f.button :submit %>
<% end %>

  </div>
</div>
